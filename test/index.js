import assert from 'assert';
import client from '../src/index';

suite('This will test graphql-js-client', () => {
  test('It should do something', () => {
    assert.ok(client, "It's good");
  });
});
